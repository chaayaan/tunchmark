<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Hallmark & Tunch Report Authenticity | Rajaiswari Gold Testing Center Chattogram</title>

    <meta name="description" content="Check if your hallmark report or tunch (gold purity) test report is genuine and authentic. Official online report verification system by Rajaiswari Gold Testing Center (রাজঐশ্বরী গোল্ড টেস্টিং সেন্টার), Chattogram, Bangladesh — enter your HUID to confirm authenticity.">

    <meta name="keywords" content="
    hallmark report verification, tunch report verification, is this hallmark report genuine, is this tunch report authentic, gold report authenticity check, HUID verification Bangladesh, hallmark report check Chattogram, tunch test report check Chittagong, Rajaiswari Gold Testing Center, রাজঐশ্বরী গোল্ড টেস্টিং সেন্টার,
    হলমার্ক রিপোর্ট আসল না নকল, টঞ্চ রিপোর্ট আসল কিনা, হলমার্ক রিপোর্ট যাচাই, টঞ্চ রিপোর্ট যাচাই, রিপোর্ট আসল কিনা যাচাই, স্বর্ণের রিপোর্ট সত্যতা যাচাই, হলমার্ক সত্যতা যাচাই চট্টগ্রাম, টঞ্চ সত্যতা যাচাই চট্টগ্রাম, HUID যাচাই, হলমার্ক রিপোর্ট চেক, টঞ্চ রিপোর্ট চেক, স্বর্ণের রিপোর্ট আসল কিনা, রিপোর্ট ভেরিফিকেশন চট্টগ্রাম, রাজঐশ্বরী রিপোর্ট যাচাই, halmark report asol na nokol, tunch report jachai, report authentic kina check
    ">

    <meta name="robots" content="index, follow, max-image-preview:large">
    <meta name="googlebot" content="index, follow">
    <link rel="canonical" href="https://rajaiswari.com/search_report.php">
    <link rel="alternate" hreflang="bn-BD" href="https://rajaiswari.com/search_report.php">
    <link rel="alternate" hreflang="en-BD" href="https://rajaiswari.com/search_report.php">
    <link rel="alternate" hreflang="x-default" href="https://rajaiswari.com/search_report.php">

    <!-- Geo targeting: Chattogram / Chittagong, Bangladesh -->
    <meta name="geo.region" content="BD-BD">
    <meta name="geo.placename" content="Chattogram, Chittagong, Bangladesh">
    <meta name="geo.position" content="22.3569;91.7832">
    <meta name="ICBM" content="22.3569, 91.7832">
    <meta name="language" content="en, bn">

    <!-- Open Graph -->
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Rajaiswari Gold Testing Center">
    <meta property="og:title" content="Verify Hallmark & Tunch Report Authenticity | Rajaiswari Gold Testing Center">
    <meta property="og:description" content="Confirm whether your hallmark or tunch (purity) test report is genuine. Official verification by Rajaiswari Gold Testing Center (রাজঐশ্বরী গোল্ড টেস্টিং সেন্টার), Chattogram — enter your HUID.">
    <meta property="og:url" content="https://rajaiswari.com/search_report.php">
    <meta property="og:image" content="https://rajaiswari.com/favicon.png">
    <meta property="og:locale" content="en_BD">
    <meta property="og:locale:alternate" content="bn_BD">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="Verify Hallmark & Tunch Report Authenticity | Rajaiswari Gold Testing Center">
    <meta name="twitter:description" content="Check if your hallmark or tunch report is genuine — official verification system by Rajaiswari Gold Testing Center, Chattogram.">

    <!-- Structured Data: LocalBusiness -->
    <script type="application/ld+json">
    {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "@id": "https://rajaiswari.com/#business",
    "name": "Rajaiswari Gold Testing Center",
    "alternateName": ["রাজঐশ্বরী গোল্ড টেস্টিং সেন্টার", "Rajaiswari Hallmark & Tunch Testing Center"],
    "description": "Rajaiswari Gold Testing Center (রাজঐশ্বরী গোল্ড টেস্টিং সেন্টার), Chattogram, Bangladesh — the only official source for verifying whether a hallmark report or tunch (gold purity) test report bearing its HUID is genuine and authentic.",
    "url": "https://rajaiswari.com/search_report.php",
    "image": "https://rajaiswari.com/favicon.png",
    "address": {
        "@type": "PostalAddress",
        "addressLocality": "Chattogram",
        "addressRegion": "Chittagong",
        "addressCountry": "BD"
    },
    "geo": {
        "@type": "GeoCoordinates",
        "latitude": "22.3569",
        "longitude": "91.7832"
    },
    "areaServed": [
        { "@type": "City", "name": "Chattogram" },
        { "@type": "City", "name": "Chittagong" }
    ],
    "knowsAbout": ["Hallmark Report Authenticity", "Tunch Report Authenticity", "Gold Purity Verification", "HUID Verification"]
    }
    </script>

    <!-- Structured Data: Service -->
    <script type="application/ld+json">
    {
    "@context": "https://schema.org",
    "@type": "Service",
    "serviceType": "Hallmark and Tunch Report Authenticity Verification",
    "provider": {
        "@id": "https://rajaiswari.com/#business"
    },
    "areaServed": "Chattogram, Chittagong, Bangladesh",
    "name": "Hallmark & Tunch Report Authenticity Verification",
    "description": "Enter your HUID to confirm whether a hallmark report or tunch (gold purity) test report was genuinely issued by Rajaiswari Gold Testing Center (রাজঐশ্বরী গোল্ড টেস্টিং সেন্টার), Chattogram. This service is exclusively for authenticity verification of reports issued by Rajaiswari.",
    "url": "https://rajaiswari.com/search_report.php"
    }
    </script>
    </script>
    <link rel="icon" type="image/png" href="favicon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&display=swap" rel="stylesheet">
    <style>
        /* ═══ CONFIG: update ASSET_BASE below (near </head>) and API_URL in <script> to your main domain ═══ */

        /* ── Variables ─────────────────────────────────── */
        :root {
            --rg-gold:        #B8881E;
            --rg-gold-light:  #D4A83A;
            --rg-gold-pale:   #F5EDD6;
            --rg-gold-border: rgba(184,136,30,0.22);
            --rg-dark:        #1C1A16;
            --rg-text:        #2E2A22;
            --rg-muted:       #7A7060;
            --rg-bg:          #FDFAF4;
            --rg-bg2:         #F7F2E8;
            --rg-white:       #FFFFFF;
            --rg-nav-h:       72px;
        }

        /* ── Reset & Base ───────────────────────────────── */
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html { scroll-behavior: smooth; font-size: 16px; overflow-x: hidden; }
        body {
            font-family: 'Outfit', sans-serif;
            background: var(--rg-bg);
            color: var(--rg-text);
            overflow-x: hidden;
            line-height: 1.6;
        }
        img { display: block; max-width: 100%; height: auto; }
        a { text-decoration: none; }

        /* ── Navbar ─────────────────────────────────────── */
        .rg-nav {
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
            height: var(--rg-nav-h);
            display: flex; align-items: center; justify-content: space-between;
            padding: 0 48px;
            background: rgba(253,250,244,0.97);
            backdrop-filter: blur(16px);
            border-bottom: 1px solid var(--rg-gold-border);
            transition: box-shadow 0.3s, height 0.3s;
        }
        .rg-nav.rg-scrolled {
            box-shadow: 0 2px 24px rgba(120,90,30,0.1);
            height: 60px;
        }
        .rg-nav-brand {
            display: flex; align-items: center; gap: 10px;
            flex-shrink: 0; text-decoration: none;
        }
        .rg-nav-brand img { width: 100px; height: 50px; object-fit: contain; display: block; }
        .rg-nav-brand-text {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.4rem; font-weight: 600;
            color: var(--rg-gold); letter-spacing: 0.04em; line-height: 1.1; white-space: nowrap;
        }
        .rg-nav-links { display: flex; gap: 28px; list-style: none; }
        .rg-nav-links a {
            color: var(--rg-muted); font-size: 0.8rem; font-weight: 500;
            letter-spacing: 0.1em; text-transform: uppercase;
            transition: color 0.3s; position: relative;
        }
        .rg-nav-links a::after {
            content: ''; position: absolute; bottom: -4px; left: 0;
            width: 0; height: 1.5px; background: var(--rg-gold); transition: width 0.3s;
        }
        .rg-nav-links a:hover { color: var(--rg-gold); }
        .rg-nav-links a:hover::after { width: 100%; }
        .rg-nav-social { display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
        .rg-social-btn {
            display: flex; align-items: center; justify-content: center;
            width: 38px; height: 38px; border-radius: 50%;
            transition: transform 0.2s, box-shadow 0.2s; flex-shrink: 0;
        }
        .rg-social-btn:hover { transform: translateY(-2px); box-shadow: 0 4px 14px rgba(0,0,0,0.18); }
        .rg-fb { background: #1877F2; }
        .rg-wp { background: #25D366; }
        .rg-hamburger {
            display: none; flex-direction: column; gap: 5px;
            cursor: pointer; padding: 4px; background: none; border: none;
        }
        .rg-hamburger span { display: block; width: 24px; height: 2px; background: var(--rg-dark); transition: all 0.3s; }
        .rg-hamburger.rg-open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
        .rg-hamburger.rg-open span:nth-child(2) { opacity: 0; }
        .rg-hamburger.rg-open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }
        .rg-mobile-menu {
            display: none; position: fixed;
            top: var(--rg-nav-h); left: 0; right: 0;
            background: rgba(253,250,244,0.98);
            backdrop-filter: blur(16px);
            border-bottom: 1px solid var(--rg-gold-border);
            padding: 20px 24px 28px;
            flex-direction: column; gap: 0;
            z-index: 999;
            box-shadow: 0 8px 32px rgba(120,90,30,0.1);
        }
        .rg-mobile-menu.rg-open { display: flex; }
        .rg-mobile-menu a {
            display: block; padding: 13px 0;
            border-bottom: 1px solid var(--rg-gold-border);
            color: var(--rg-text); font-size: 0.9rem;
            font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase;
            transition: color 0.3s;
        }
        .rg-mobile-menu a:hover { color: var(--rg-gold); }
        .rg-mob-social { display: flex; gap: 12px; margin-top: 18px; }
        .rg-mob-social a {
            flex: 1; display: flex; align-items: center; justify-content: center; gap: 8px;
            padding: 16px 12px; border-bottom: none !important;
            color: #fff !important; font-size: 0.88rem; font-weight: 600;
            letter-spacing: 0.06em; border-radius: 6px;
            transition: opacity 0.2s; min-height: 52px;
            position: relative; z-index: 10; pointer-events: all !important;
            -webkit-tap-highlight-color: rgba(0,0,0,0.1);
            touch-action: manipulation; cursor: pointer;
        }
        .rg-mob-social a:hover, .rg-mob-social a:active { opacity: 0.88; }
        .rg-mob-social a.rg-fb { background: #1877F2; }
        .rg-mob-social a.rg-wp { background: #25D366; }
        .rg-mob-social a svg { flex-shrink: 0; pointer-events: none; }

        /* ── WhatsApp Floating Button ───────────────────── */
        .rg-wa-wrap { position: fixed; bottom: 28px; right: 28px; z-index: 9999; display: flex; flex-direction: column; align-items: flex-end; gap: 12px; }
        .rg-wa-popup { width: 300px; background: #fff; border-radius: 16px; box-shadow: 0 12px 48px rgba(0,0,0,0.16), 0 2px 8px rgba(0,0,0,0.08); overflow: hidden; opacity: 0; transform: translateY(16px) scale(0.96); pointer-events: none; transition: opacity 0.3s ease, transform 0.3s ease; transform-origin: bottom right; }
        .rg-wa-popup.rg-wa-open { opacity: 1; transform: translateY(0) scale(1); pointer-events: all; }
        .rg-wa-close { position: absolute; top: 10px; right: 12px; width: 22px; height: 22px; background: rgba(255,255,255,0.2); border: none; border-radius: 50%; cursor: pointer; display: flex; align-items: center; justify-content: center; color: #fff; transition: background 0.2s; z-index: 2; }
        .rg-wa-close:hover { background: rgba(255,255,255,0.35); }
        .rg-wa-popup-head { background: linear-gradient(135deg, #25D366 0%, #128C7E 100%); padding: 18px 16px 16px; display: flex; align-items: center; gap: 12px; position: relative; }
        .rg-wa-avatar { width: 44px; height: 44px; border-radius: 50%; background: rgba(255,255,255,0.2); display: flex; align-items: center; justify-content: center; flex-shrink: 0; position: relative; }
        .rg-wa-online-dot { position: absolute; bottom: 1px; right: 1px; width: 10px; height: 10px; border-radius: 50%; background: #4ADE80; border: 2px solid #25D366; animation: rg-wa-pulse 2s ease-in-out infinite; }
        @keyframes rg-wa-pulse { 0%,100% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.2); opacity: 0.8; } }
        .rg-wa-popup-info { flex: 1; min-width: 0; }
        .rg-wa-popup-name { display: block; color: #fff; font-weight: 600; font-size: 0.92rem; letter-spacing: 0.02em; line-height: 1.2; }
        .rg-wa-popup-status { display: flex; align-items: center; gap: 5px; color: rgba(255,255,255,0.82); font-size: 0.7rem; margin-top: 3px; }
        .rg-wa-status-dot { width: 7px; height: 7px; border-radius: 50%; background: #4ADE80; flex-shrink: 0; }
        .rg-wa-popup-body { padding: 16px; background: #E5DDD5; }
        .rg-wa-bubble { background: #fff; border-radius: 0 12px 12px 12px; padding: 10px 14px 8px; max-width: 85%; box-shadow: 0 1px 4px rgba(0,0,0,0.1); position: relative; }
        .rg-wa-bubble::before { content: ''; position: absolute; top: 0; left: -8px; border: 8px solid transparent; border-top-color: #fff; border-left: 0; }
        .rg-wa-bubble p { font-size: 0.84rem; color: #2E2A22; line-height: 1.55; margin-bottom: 4px; }
        .rg-wa-bubble p:last-of-type { margin-bottom: 0; }
        .rg-wa-time { display: block; text-align: right; font-size: 0.62rem; color: #999; margin-top: 6px; }
        .rg-wa-popup-btn { display: flex; align-items: center; justify-content: center; gap: 8px; width: 100%; padding: 14px; background: linear-gradient(135deg, #25D366 0%, #128C7E 100%); color: #fff; font-size: 0.82rem; font-weight: 600; letter-spacing: 0.06em; text-transform: uppercase; transition: opacity 0.2s, transform 0.2s; border: none; cursor: pointer; }
        .rg-wa-popup-btn:hover { opacity: 0.92; transform: translateY(-1px); }
        .rg-wa-fab { width: auto; min-width: 56px; height: 56px; background: linear-gradient(135deg, #25D366 0%, #128C7E 100%); border: none; border-radius: 100px; display: flex; align-items: center; gap: 10px; padding: 0 20px 0 16px; cursor: pointer; box-shadow: 0 4px 20px rgba(37,211,102,0.4); position: relative; transition: transform 0.25s ease, box-shadow 0.25s ease, padding 0.25s ease; overflow: hidden; }
        .rg-wa-fab:hover { transform: translateY(-3px) scale(1.03); box-shadow: 0 8px 28px rgba(37,211,102,0.5); }
        .rg-wa-fab:active { transform: scale(0.97); }
        .rg-wa-fab-icon { display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: opacity 0.2s, transform 0.2s; }
        .rg-wa-fab-icon--close { position: absolute; left: 16px; opacity: 0; transform: rotate(-90deg); }
        .rg-wa-fab.rg-wa-active .rg-wa-fab-icon--wa   { opacity: 0; transform: rotate(90deg); }
        .rg-wa-fab.rg-wa-active .rg-wa-fab-icon--close { opacity: 1; transform: rotate(0); }
        .rg-wa-label { color: #fff; font-size: 0.8rem; font-weight: 600; letter-spacing: 0.06em; white-space: nowrap; transition: opacity 0.2s, max-width 0.3s; max-width: 120px; overflow: hidden; }
        .rg-wa-fab.rg-wa-active .rg-wa-label { opacity: 0; max-width: 0; }
        .rg-wa-fab-ping { position: absolute; inset: 0; border-radius: 100px; border: 2px solid rgba(37,211,102,0.6); animation: rg-wa-ring 2.5s ease-out infinite; pointer-events: none; }
        @keyframes rg-wa-ring { 0% { transform: scale(1); opacity: 0.7; } 70%,100% { transform: scale(1.35); opacity: 0; } }

        /* ── Page layout ────────────────────────────────── */
        .rg-page-body {
            padding-top: calc(var(--rg-nav-h) + 10px);
            padding-bottom: 24px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            background: #f1f3f6;
            width: 100%;
            overflow-x: hidden;
        }

        /* ── Search bar ─────────────────────────────────── */
        .search-bar-wrap{
            width:100%;max-width:520px;margin:0 auto 20px;padding:0 16px;
            display:flex;flex-direction:column;gap:10px;
        }
        .search-title{font-family:'Cormorant Garamond',serif;font-size:1.75rem;font-weight:700;color:var(--rg-gold);text-align:center;}
        .huid-label{display:none;}
        .search-row{display:flex;gap:8px;align-items:center;}
        .search-input{
            flex:1;height:42px;padding:0 12px;border:1.5px solid var(--rg-gold-border);border-radius:8px;
            font-family:'Outfit',sans-serif;font-size:0.9rem;letter-spacing:.05em;
            outline:none;transition:border-color .15s;background:#fff;color:var(--rg-text);
            min-width:0;
        }
        .search-input:focus{border-color:var(--rg-gold);}
        .search-btn{
            height:42px;padding:0 18px;border:none;border-radius:8px;background:var(--rg-gold);
            color:#fff;font-weight:600;font-size:.85rem;cursor:pointer;transition:background .15s;white-space:nowrap;
            flex-shrink:0;display:flex;align-items:center;justify-content:center;gap:6px;
        }
        .search-btn:hover{background:#9c7118;}
        .search-btn:disabled{background:#c9b98a;cursor:not-allowed;}
        .search-btn svg{flex-shrink:0;}
        .scan-btn{
            height:42px;padding:0 16px;border:none;border-radius:8px;background:#25D366;
            color:#fff;font-weight:600;font-size:.85rem;cursor:pointer;transition:background .15s,opacity .15s;
            white-space:nowrap;flex-shrink:0;display:flex;align-items:center;justify-content:center;gap:6px;
        }
        .scan-btn:hover{background:#1fb457;}
        .scan-btn:disabled{background:#a9d9bd;cursor:not-allowed;}
        .scan-btn svg{flex-shrink:0;}
        .search-hint{text-align:center;font-size:.72rem;color:var(--rg-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
        .loading{text-align:center;color:var(--rg-muted);font-size:.9rem;margin-top:10px;}

        /* ── Initial info placeholder (shown before any HUID is searched) ── */
        .info-placeholder{
            width:100%;max-width:440px;margin:0 auto;padding:0 26px 22px;
            display:flex;flex-direction:column;align-items:center;gap:10px;
            text-align:center;opacity:.55;transition:opacity .2s;
        }
        .info-placeholder:hover{opacity:.8;}
        .info-placeholder svg{color:var(--rg-gold);flex-shrink:0;}
        .info-placeholder p{
            font-size:.82rem;line-height:1.65;color:var(--rg-muted);font-weight:400;
            max-width:360px;
        }
        .info-placeholder p strong{color:var(--rg-text);font-weight:600;}
        @media (max-width:600px){
            .info-placeholder{padding:0 20px 16px;max-width:320px;}
            .info-placeholder p{font-size:.78rem;}
        }

        /* ── QR Scanner Modal ───────────────────────────── */
        .scan-modal-overlay{
            display:none;position:fixed;inset:0;z-index:10000;
            background:rgba(10,10,10,0.88);
            align-items:center;justify-content:center;padding:16px;
        }
        .scan-modal-overlay.rg-open{display:flex;}
        .scan-modal-box{
            width:100%;max-width:380px;background:#fff;border-radius:14px;
            overflow:hidden;box-shadow:0 20px 60px rgba(0,0,0,0.4);
        }
        .scan-modal-head{
            padding:14px 18px;display:flex;align-items:center;justify-content:space-between;
            background:var(--rg-dark);
        }
        .scan-modal-title{color:#fff;font-weight:600;font-size:0.92rem;letter-spacing:.02em;}
        .scan-modal-close{
            background:none;border:none;color:#fff;font-size:1.4rem;line-height:1;
            cursor:pointer;padding:2px 6px;border-radius:6px;transition:background .15s;
        }
        .scan-modal-close:hover{background:rgba(255,255,255,0.12);}
        #qrReaderBox{width:100%;background:#000;min-height:260px;}
        #qrReaderBox video{width:100%!important;}
        .scan-modal-msg{
            padding:10px 18px;font-size:0.8rem;text-align:center;min-height:20px;
            font-family:'Outfit',sans-serif;
        }
        .scan-modal-msg.rg-error{color:#dc2626;}
        .scan-modal-msg.rg-info{color:var(--rg-muted);}
        .scan-modal-hint{
            padding:0 18px 16px;font-size:0.72rem;color:var(--rg-muted);text-align:center;
        }

        /* ── Report Card Styles ─────────────────────────── */
        .report-card-wrapper {
            width: 100%;
            display: flex;
            justify-content: center;
            padding: 0 16px;
        }
        @media (max-width: 600px) {
            .report-card-wrapper { padding: 0; }
        }
        #resultArea {
            width: 100%;
            flex: 1 1 auto;
            display: flex;
            align-items: flex-start;
            justify-content: center;
        }

        *, *::before, *::after {
            -webkit-user-select: none; -moz-user-select: none;
            -ms-user-select: none;
        }
        img { pointer-events: none; -webkit-user-drag: none; }

        /* ── Scale wrapper: centers the (possibly shrunk) report card and
               never lets it force horizontal scroll on the page ── */
        .scale-wrapper {
            width: 100%;
            max-width: 100vw;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            overflow: hidden;
        }
        /* transform-origin is top-left because the JS resizes the LAYOUT
           box (#scaleInner width/height) to match the scaled visual size —
           scaling from the corner keeps that box calculation simple and
           avoids leftover blank space on the right on mobile */
        .scale-inner { transform-origin: top left; }

        .report-card {
            width: 680px;
            background: transparent;
            border: none;
            box-shadow: none;
            border-radius: 10px;
            overflow: hidden;
            position: relative;
        }
        .report-card.hallmark { width: 750px; }
        .report-pad-bg {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            object-fit: fill;
            z-index: 0;
            pointer-events: none;
            display: block;
        }
        .report-card-content {
            position: relative;
            z-index: 1;
        }

        .report-card-content {
            padding-top: 90px;    /* adjust to clear the pad's header area */
            padding-bottom: 70px; /* adjust to clear the pad's footer area */
        }

        .tunch-container {
            padding: 16px 28px 14px;
            background: white;
            position: relative;
        }
        .tunch-container::before {
            content: ''; position: absolute; top: 50%; left: 50%;
            transform: translate(-50%, -50%) rotate(-25deg);
            width: 200px; height: 200px;
            background-image: url('Varifiedstamp.png');
            background-size: contain; background-repeat: no-repeat;
            background-position: center; opacity: 0.2; z-index: 1; pointer-events: none;
        }
        .tunch-container > * { position: relative; z-index: 2; }

        .report-header { display: flex; justify-content: space-between; align-items: flex-start; }
        .customer-info { flex: 1; }

        .customer-info-line { display: flex; font-size: 15px; line-height: 1.8; color: #000; font-weight: 600; }
        .customer-info-line.customer-name { font-size: 20px; font-weight: bold; margin-bottom: 3px; }
        .info-label  { display: inline-block; min-width: 120px; font-weight: 600; }
        .info-colon  { display: inline-block; width: 15px; text-align: center; }
        .info-value  { flex: 1; font-weight: 600; }
        .weight-conversion { font-size: 13px; color: #333; font-weight: 600; margin-left: 10px; }

        .qr-section { width: 100px; text-align: center; padding: 5px; margin-left: 15px; flex-shrink: 0; }
        .qr-date { font-size: 11px; color: #000; font-weight: 700; line-height: 1.4; margin-top: 5px; white-space: nowrap; font-family: 'Times New Roman', Times, serif; }

        .dotted-line { border-top: 3px dotted #000; margin: 6px 0; }

        .quality-info {
            font-size: 22px; font-weight: bold; margin: 5px 0; line-height: 1.5;
            display: flex; justify-content: space-around; flex-wrap: wrap; gap: 20px; color: #000;
        }
        .quality-info span { white-space: nowrap; }

        .composition-table { width: 100%; margin: 2px 0 0; font-size: 11px; line-height: 1.1; border-collapse: collapse; }
        .composition-table td { padding: 1px 5px; font-weight: 600; vertical-align: top; }
        .composition-table td.element-name  { text-align: left; padding-right: 3px; }
        .composition-table td.element-colon { text-align: center; padding: 1px 2px; }
        .composition-table td.element-value { text-align: left; padding-left: 3px; padding-right: 15px; }

        .report-note { font-size: 11px; line-height: 1.4; margin: 4px 0 0; font-weight: 600; color: #000; font-family: 'Times New Roman', Times, serif; }

        .verified-banner {
            background: #4fa756; color: #fff; text-align: center;
            padding: 14px 20px; font-size: 15px; font-weight: 700;
            font-family: 'Times New Roman', Times, serif; letter-spacing: .5px;
        }
        .report-footer-note {
            background: #f8f9fa; border-top: 1px solid #e4e7ec;
            padding: 14px 28px; text-align: center;
            font-size: 12px; color: #6b7280; font-family: 'Times New Roman', Times, serif;
        }

        .error-box { width: 90%; max-width: 500px; background: #fff; border: 1px solid #e4e7ec; border-radius: 10px; box-shadow: 0 1px 3px rgba(0,0,0,.06); padding: 0 40px 40px; text-align: center; }
        .error-icon { font-size: 64px; margin-bottom: 20px; }
        .error-box h2 { color: #dc2626; font-size: 22px; margin-bottom: 12px; font-family: sans-serif; }
        .error-box p  { color: #6b7280; font-size: 14px; line-height: 1.7; font-family: sans-serif; }

        .hallmark-info-section { display: flex; justify-content: space-between; align-items: flex-start; padding: 3px 8px; }
        .hallmark-left-info { flex: 1; line-height: 1.2; }
        .hallmark-left-info .customer-info-line { font-size: 15px; line-height: 1.4; display: block; }
        .hallmark-left-info .customer-info-line.customer-name { font-size: 20px; font-weight: bold; margin-bottom: 2px; }
        .hallmark-left-info .info-label { display: inline-block; min-width: 110px; font-weight: 600; }
        .hallmark-left-info .info-colon { margin: 0 3px; }
        .hallmark-left-info .info-value  { font-weight: 600; }

        .main-box { border: 2.5px solid #000; display: flex; margin: 0 8px 0; min-height: 100px; }
        .checkbox-section { flex: 1; padding: 8px 12px; display: grid; grid-template-columns: repeat(4, 1fr); gap: 5px 10px; align-content: center; }
        .checkbox-item { display: flex; align-items: center; font-size: 12px; line-height: 1; font-weight: 600; color: #000; }
        .checkbox-box { width: 14px; height: 14px; border: 2px solid #000; margin-right: 4px; flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 12px; font-weight: bold; line-height: 1; }
        .hallmark-section { width: 220px; border-left: 2.5px solid #000; display: flex; flex-direction: column; }
        .hallmark-value-container { flex: 1; display: flex; align-items: center; justify-content: center; border-bottom: 2.5px solid #000; padding: 8px 12px; overflow: hidden; }
        .hallmark-value { font-size: 38px; font-weight: bold; line-height: 1; color: #000; text-align: center; word-wrap: break-word; word-break: break-word; font-family: 'Times New Roman', Times, serif; }
        .hallmark-label { font-size: 15px; font-weight: 700; text-align: center; padding: 4px; color: #000; line-height: 1; font-family: 'Times New Roman', Times, serif; }

        /* ── Footer ─────────────────────────────────────── */
        .rg-footer {
            background: var(--rg-dark);
            padding: 60px clamp(20px, 6vw, 80px) 0;
            margin-top: 60px;
        }
        .rg-footer-top {
            display: grid;
            grid-template-columns: 1.6fr 1fr 1fr 1.4fr;
            gap: 48px;
            padding-bottom: 48px;
            border-bottom: 1px solid rgba(253,250,244,0.08);
        }
        .rg-footer-brand { display: flex; align-items: center; gap: 10px; margin-bottom: 16px; }
        .rg-footer-brand img { width: 90px; height: 45px; object-fit: contain; opacity: 0.9; }
        .rg-footer-brand-fallback { font-family: 'Cormorant Garamond', serif; font-size: 1.3rem; font-weight: 600; color: var(--rg-gold-light); letter-spacing: 0.05em; }
        .rg-footer-tagline { color: rgba(253,250,244,0.38); font-size: 0.78rem; font-weight: 300; line-height: 1.75; margin-bottom: 20px; }
        .rg-footer-map { width: 100%; border-radius: 8px; overflow: hidden; border: 1px solid rgba(184,136,30,0.2); margin-bottom: 20px; opacity: 0.85; transition: opacity 0.2s; }
        .rg-footer-map:hover { opacity: 1; }
        .rg-footer-map iframe { display: block; }
        .rg-footer-socials { display: flex; gap: 10px; }
        .rg-footer-social { width: 36px; height: 36px; border-radius: 50%; display: flex; align-items: center; justify-content: center; transition: transform 0.2s, box-shadow 0.2s, opacity 0.2s; }
        .rg-footer-social:hover { transform: translateY(-2px); opacity: 0.9; box-shadow: 0 4px 14px rgba(0,0,0,0.3); }
        .rg-footer-social-fb { background: #1877F2; }
        .rg-footer-social-wa { background: #25D366; }
        .rg-footer-col-title { font-family: 'Outfit', sans-serif; font-size: 0.65rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: var(--rg-gold-light); margin-bottom: 18px; padding-bottom: 10px; border-bottom: 1px solid rgba(184,136,30,0.2); }
        .rg-footer-linklist { list-style: none; display: flex; flex-direction: column; gap: 10px; }
        .rg-footer-linklist a { color: rgba(253,250,244,0.42); font-size: 0.8rem; font-weight: 300; letter-spacing: 0.04em; transition: color 0.25s, padding-left 0.25s; display: inline-block; }
        .rg-footer-linklist a:hover { color: var(--rg-gold-light); padding-left: 4px; }
        .rg-footer-contact-list { list-style: none; display: flex; flex-direction: column; gap: 12px; }
        .rg-footer-contact-list li { display: flex; align-items: flex-start; gap: 9px; color: rgba(253,250,244,0.42); font-size: 0.78rem; font-weight: 300; line-height: 1.6; }
        .rg-footer-contact-list svg { color: var(--rg-gold); flex-shrink: 0; margin-top: 3px; }
        .rg-footer-contact-list a { color: rgba(253,250,244,0.42); transition: color 0.25s; }
        .rg-footer-contact-list a:hover { color: var(--rg-gold-light); }
        .rg-footer-bottom { display: flex; justify-content: space-between; flex-wrap: wrap; gap: 8px; padding: 20px 0; }
        .rg-footer-copy, .rg-footer-credit { font-size: 0.65rem; color: rgba(253,250,244,0.18); letter-spacing: 0.04em; }

        /* ── Responsive ─────────────────────────────────── */
        @media (max-width: 900px) {
            .rg-nav { padding: 0 20px; }
            .rg-nav-links, .rg-nav-social { display: none; }
            .rg-hamburger { display: flex; }
            .rg-footer-top { grid-template-columns: 1fr 1fr; gap: 36px; }
            .rg-footer-col-brand { grid-column: span 2; }
        }
        @media (max-width: 600px) {
            :root { --rg-nav-h: 64px; }
            .rg-wa-wrap { bottom: 18px; right: 16px; }
            .rg-wa-popup { width: 272px; }
            .rg-wa-fab { height: 52px; padding: 0 16px 0 13px; }
            .rg-wa-label { font-size: 0.75rem; }
            .rg-footer-top { grid-template-columns: 1fr 1fr; gap: 28px; }
            .rg-footer-col-brand { grid-column: span 2; }
            .rg-footer-bottom { flex-direction: column; gap: 4px; }
            /* Tighter search bar on small phones */
            .rg-page-body { padding-top: calc(var(--rg-nav-h) + 12px); }
            .search-bar-wrap { margin-bottom: 14px; gap: 8px; padding: 0 12px; }
            .search-title { display: none; }
            .search-row { gap: 6px; }
            .huid-label {
                display: inline-block; flex-shrink: 0;
                font-family: 'Outfit', sans-serif; font-weight: 700; font-size: 0.8rem;
                letter-spacing: 0.04em; color: var(--rg-gold);
            }
            .search-input { height: 40px; font-size: 0.85rem; padding: 0 10px; }
            .search-btn, .scan-btn {
                width: 40px; height: 40px; padding: 0; flex-shrink: 0;
            }
            .search-btn .btn-text, .scan-btn .btn-text { display: none; }
            .search-hint { font-size: 0.68rem; }
        }
    </style>
</head>
<body oncontextmenu="return false;" oncopy="return false;" oncut="return false;" onpaste="return false;">

<?php include 'header.php'; ?>

<!-- ══ PAGE BODY ══ -->
<div class="rg-page-body">

    <div class="search-bar-wrap">
        <div class="search-title">Verify Your Report</div>
        <div class="search-row">
            <span class="huid-label">HUID</span>
            <input type="text" id="huidInput" class="search-input" placeholder="Enter HUID e.g. 2226B9" maxlength="6">
            <button id="searchBtn" class="search-btn" title="Search">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>
                </svg>
                <span class="btn-text">Search</span>
            </button>
            <button id="scanBtn" class="scan-btn" title="Scan QR code with camera">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M3 7V5a2 2 0 0 1 2-2h2"/><path d="M17 3h2a2 2 0 0 1 2 2v2"/>
                    <path d="M21 17v2a2 2 0 0 1-2 2h-2"/><path d="M7 21H5a2 2 0 0 1-2-2v-2"/>
                    <rect x="7" y="7" width="10" height="10" rx="1"/>
                </svg>
                <span class="btn-text">Scan</span>
            </button>
        </div>
        <div class="search-hint">Enter HUID or scan the QR code on your report</div>
    </div>

    <div id="resultArea"></div>

</div><!-- /rg-page-body -->

<!-- ══ QR SCANNER MODAL ══ -->
<div class="scan-modal-overlay" id="scanModal">
    <div class="scan-modal-box">
        <div class="scan-modal-head">
            <span class="scan-modal-title">Scan Report QR Code</span>
            <button class="scan-modal-close" id="scanCloseBtn" aria-label="Close scanner">&times;</button>
        </div>
        <div id="qrReaderBox"></div>
        <div class="scan-modal-msg rg-info" id="scanMsg">Point your camera at the QR code on the report.</div>
        <div class="scan-modal-hint">Only valid Rajaiswari report QR codes will be accepted.</div>
    </div>
</div>

<?php include 'footer.php'; ?>

<!-- ══ SCRIPTS ══ -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/html5-qrcode/2.3.8/html5-qrcode.min.js"></script>
<script>
    // ═══ CONFIG: point these at your main domain ═══
    const API_URL    = 'https://app.rajaiswari.com/api_report_lookup.php';
    const ASSET_BASE = ''; // for report pad.png / Varifiedstamp.png

    /* ── Anti-copy ── */
    document.addEventListener('keydown', function(e) {
        if (
            (e.ctrlKey && ['c','x','s','a','p','u'].includes(e.key.toLowerCase())) ||
            (e.ctrlKey && e.shiftKey && ['i','j','c'].includes(e.key.toLowerCase())) ||
            e.key === 'F12' || e.key === 'PrintScreen'
        ) { e.preventDefault(); return false; }
    });
    document.addEventListener('keyup', function(e) {
        if (e.key === 'PrintScreen') navigator.clipboard.writeText('').catch(function(){});
    });
    document.addEventListener('selectstart', function(e) { e.preventDefault(); });
    document.addEventListener('dragstart',   function(e) { e.preventDefault(); });
    document.addEventListener('copy',        function(e) { e.preventDefault(); });
    document.addEventListener('cut',         function(e) { e.preventDefault(); });

    /* ══════════════════════════════════════════════════════════════════
       SEARCH + RENDER (all data via API — same UI as report_varification.php)
       ══════════════════════════════════════════════════════════════════ */

    const input      = document.getElementById('huidInput');
    const btn        = document.getElementById('searchBtn');
    const resultArea = document.getElementById('resultArea');

    // Note: intentionally NOT forcing uppercase here — HUID codes are
    // case-sensitive (charset includes both e.g. 'P' and 'p' as distinct
    // symbols), so rewriting the user's typed case would make some valid
    // HUIDs impossible to enter.
    input.addEventListener('keydown', e => { if (e.key === 'Enter') doSearch(); });
    // Only allow characters from the approved HUID charset — strips
    // emoji, symbols, spaces, or anything else on type/paste, in real time.
    const HUID_ALLOWED_CHARS = new Set('23456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz');

    input.addEventListener('input', function() {
        const cleaned = [...this.value].filter(ch => HUID_ALLOWED_CHARS.has(ch)).join('');
        if (cleaned !== this.value) this.value = cleaned;
    });

    input.addEventListener('paste', function(e) {
        e.preventDefault();
        const pasted = (e.clipboardData || window.clipboardData).getData('text');
        const cleaned = [...pasted].filter(ch => HUID_ALLOWED_CHARS.has(ch)).join('').slice(0, 6);
        const start = this.selectionStart, end = this.selectionEnd;
        this.value = this.value.slice(0, start) + cleaned + this.value.slice(end);
        this.dispatchEvent(new Event('input'));
    });
    btn.addEventListener('click', () => doSearch());

    function escapeHtml(str) {
        const d = document.createElement('div');
        d.textContent = str ?? '';
        return d.innerHTML;
    }

    function convertGramToVoriAna(gram) {
        gram = parseFloat(gram);
        if (!gram || gram <= 0) return '';
        const tp = Math.round((gram / 11.664) * 16 * 6 * 10);
        const b = Math.floor(tp / 960), r1 = tp % 960;
        const a = Math.floor(r1 / 60), r2 = r1 % 60;
        const ro = Math.floor(r2 / 10), p = r2 % 10;
        return ` [V:${b} A:${a} R:${ro} P:${p}]`;
    }

    async function doSearch(huidFromUrl) {
        const huid = (huidFromUrl || input.value).trim();
        input.value = huid;

        if (huid.length !== 6) {
            renderError('Please enter a valid 6-character HUID.');
            return;
        }

        btn.disabled = true;
        resultArea.innerHTML = '<div class="loading">Looking up report...</div>';

        try {
            const res  = await fetch(`${API_URL}?huid=${encodeURIComponent(huid)}`);
            const data = await res.json();
            if (!data.success) {
                renderError(data.message || 'Report not found.');
            } else {
                renderReport(data);
                // Make the URL shareable / scannable — update it without reloading
                const newUrl = `${location.origin}${location.pathname}?huid=${huid}`;
                history.replaceState(null, '', newUrl);
            }
        } catch (err) {
            renderError('Could not reach the verification server. Please try again.');
        } finally {
            btn.disabled = false;
        }
    }

    function renderInitialInfo() {
        resultArea.innerHTML = `
            <div class="info-placeholder">
                <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10Z"/>
                    <path d="M9.5 12.5l1.8 1.8L15 10.5"/>
                </svg>
                <p>Every genuine Rajaiswari report has a unique <strong>HUID</strong>. Search or scan it here to confirm your hallmark or tunch report is authentic — always verify before buying jewellery.</p>
            </div>`;
    }

    function renderError(msg) {
        resultArea.innerHTML = `
            <div class="report-card-wrapper">
                <div class="error-box">
                    <div class="error-icon">❌</div>
                    <h2>Report Not Found</h2>
                    <p>${escapeHtml(msg)}</p>
                    <p style="margin-top:14px;">Please verify the QR code or HUID and try again.</p>
                </div>
            </div>`;
    }

    function renderReport(d) {
        const verifyUrl = `${location.origin}${location.pathname}?huid=${d.huid}`;
        const cardWidth = d.report_type === 'hallmark' ? 750 : 680;
        let innerHtml;

        if (d.report_type === 'hallmark') {
            const checkboxItems = ['anklet','bangle','bracelet','chain','ear chain','earrings','mantasha','necklace','nose pin','others','pendant','ring','shakha path','taira','tikli','watch'];
            const itemLower = (d.item_name || '').toLowerCase();
            const boxesHtml = checkboxItems.map(t => {
                const match = itemLower === t || itemLower.includes(' '+t+' ') || itemLower.startsWith(t+' ') || itemLower.endsWith(' '+t);
                const label = t.replace(/\b\w/g, c => c.toUpperCase());
                return `<div class="checkbox-item" data-item="${t}"><div class="checkbox-box">${match ? '✓' : ''}</div><span>${label}</span></div>`;
            }).join('');

            const photoBlock = d.images[0]
                ? `<div style="display:flex;gap:0;margin:6px 8px 4px;align-items:stretch;min-height:90px;">
                       <div style="flex:0 0 58.333%;max-width:58.333%;display:flex;align-items:flex-end;padding-right:8px;">
                           <img src="${escapeHtml(d.images[0])}" alt="Sample photo" style="width:auto;height:105px;object-fit:contain;border-radius:4px;border:1px solid #ddd;">
                       </div>
                       <div style="flex:0 0 41.667%;max-width:41.667%;display:flex;flex-direction:column;justify-content:flex-end;">
                           <div style="text-align:center;font-size:11px;font-weight:bold;color:#000;padding-bottom:2px;">Authorized Signature</div>
                       </div>
                   </div>`
                : `<div style="text-align:center;font-size:11px;font-weight:bold;color:#000;margin:6px 8px 4px;padding-bottom:2px;">Authorized Signature</div>`;

            innerHtml = `
                <div style="background:transparent;position:relative;">
                    <div style="position:absolute;top:50%;left:50%;
                                transform:translate(-50%,-50%) rotate(-25deg);
                                width:250px;height:250px;
                                background-image:url('${ASSET_BASE}Varifiedstamp.png');
                                background-size:contain;background-repeat:no-repeat;
                                background-position:center;opacity:0.25;
                                z-index:1;pointer-events:none;"></div>
                    <div style="position:relative;z-index:2;">
                        <div class="hallmark-info-section">
                            <div class="hallmark-left-info">
                                <div class="customer-info-line customer-name"><span>Customer Name</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.customer_name)}</span></div>
                                <div class="customer-info-line"><span class="info-label">Bill No</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.order_id)}</span></div>
                                <div class="customer-info-line"><span class="info-label">HUID</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.huid)}</span></div>
                                <div class="customer-info-line"><span class="info-label">Quantity</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.quantity || '1')}</span></div>
                                <div class="customer-info-line"><span class="info-label">Weight</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.weight)} Gm<span class="weight-conversion">${convertGramToVoriAna(d.weight)}</span></span></div>
                                <div class="customer-info-line"><span class="info-label">Made by</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.manufacturer || 'N/A')}</span></div>
                                <div class="customer-info-line"><span class="info-label">Address</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.address || 'N/A')}</span></div>
                            </div>
                            <div class="qr-section">
                                <div id="qrcode"></div>
                                <div class="qr-date">${new Date(d.created_at).toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'2-digit'})}<br>${new Date(d.created_at).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})}</div>
                            </div>
                        </div>
                        <div class="main-box">
                            <div class="checkbox-section" id="checkboxSection">${boxesHtml}</div>
                            <div class="hallmark-section">
                                <div class="hallmark-value-container"><div class="hallmark-value">${escapeHtml(d.hallmark)}</div></div>
                                <div class="hallmark-label">HallMark</div>
                            </div>
                        </div>
                        ${photoBlock}
                    </div>
                </div>`;
        } else {
            const elementRows = [];
            for (let i = 0; i < d.elements.length; i += 3) elementRows.push(d.elements.slice(i, i+3));
            const compHtml = elementRows.map(row => {
                let cells = row.map(el => `<td class="element-name">${escapeHtml(el.name)}</td><td class="element-colon">:</td><td class="element-value">${el.value === null ? '--------%' : el.value.toFixed(3)+'%'}</td>`).join('');
                for (let i = row.length; i < 3; i++) cells += `<td class="element-name"></td><td class="element-colon"></td><td class="element-value"></td>`;
                return `<tr>${cells}</tr>`;
            }).join('');

            const photosHtml = d.images.map(img =>
                `<img src="${escapeHtml(img)}" alt="Sample photo" style="width:auto;height:95px;object-fit:contain;border-radius:4px;border:1px solid #ddd;">`
            ).join('');

            innerHtml = `
                <div class="tunch-container" style="background-image:none;">
                    <style>.tunch-container::before{background-image:url('${ASSET_BASE}Varifiedstamp.png') !important;}</style>
                    <div class="report-header">
                        <div class="customer-info">
                            <div class="customer-info-line customer-name">Customer Name : ${escapeHtml(d.customer_name)}</div>
                            <div class="customer-info-line"><span class="info-label">Sample Item</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.item_name)}</span></div>
                            <div class="customer-info-line"><span class="info-label">Sample Weight</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.weight)} Gm<span class="weight-conversion">${convertGramToVoriAna(d.weight)}</span></span></div>
                            <div class="customer-info-line"><span class="info-label">Bill No</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.order_id)}</span></div>
                            <div class="customer-info-line"><span class="info-label">HUID</span><span class="info-colon">:</span><span class="info-value">${escapeHtml(d.huid)}</span></div>
                        </div>
                        <div class="qr-section">
                            <div id="qrcode"></div>
                            <div class="qr-date">${new Date(d.created_at).toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'2-digit'})}<br>${new Date(d.created_at).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})}</div>
                        </div>
                    </div>
                    <div class="dotted-line"></div>
                    <div class="quality-info">
                        <span>${escapeHtml(d.purity_label)} : ${d.purity_value !== null ? parseFloat(d.purity_value).toFixed(2) : 'N/A'}%</span>
                        <span>Karat : ${escapeHtml(d.karat ?? 'N/A')}K</span>
                    </div>
                    <div class="dotted-line"></div>
                    <table class="composition-table">${compHtml}</table>
                    <div class="report-note">NB:- The report pertains to specific point and not responsible for other point or melting issues.</div>
                    <div style="display:flex;gap:0;margin-top:6px;align-items:stretch;min-height:90px;">
                        <div style="flex:0 0 58.333%;max-width:58.333%;display:flex;align-items:flex-end;gap:8px;flex-wrap:wrap;padding-right:8px;">
                            ${photosHtml}
                        </div>
                        <div style="flex:0 0 41.667%;max-width:41.667%;display:flex;flex-direction:column;justify-content:space-between;">
                            <div style="display:flex;justify-content:flex-end;gap:18px;font-size:11px;font-weight:bold;color:#000;">
                                ${d.gold !== null ? `<span>Gold : ${parseFloat(d.gold).toFixed(3)}</span>` : ''}
                                ${d.joint !== null ? `<span>Joint : ${parseFloat(d.joint).toFixed(3)}</span>` : ''}
                            </div>
                            <div style="text-align:center;font-size:11px;font-weight:bold;color:#000;padding-bottom:2px;">Authorized Signature</div>
                        </div>
                    </div>
                </div>`;
        }

        resultArea.innerHTML = `
            <div class="report-card-wrapper">
                <div class="scale-wrapper">
                    <div class="scale-inner" id="scaleInner">
                        <div class="report-card ${d.report_type === 'hallmark' ? 'hallmark' : ''}" id="reportCard">
                            <img src="${ASSET_BASE}report pad.png" alt="" class="report-pad-bg" aria-hidden="true">
                            <div class="report-card-content">${innerHtml}</div>
                        </div>
                    </div>
                </div>
            </div>`;

        new QRCode(document.getElementById('qrcode'), { text: verifyUrl, width: 90, height: 90 });

        /* ── Scale card to fill the available mobile width (no leftover
               blank space) while staying centered and fully visible ── */
        function scaleCard() {
            var inner = document.getElementById('scaleInner');
            var card  = document.getElementById('reportCard');
            if (!inner || !card) return;

            var vw      = document.documentElement.clientWidth;
            var sidePad = vw <= 600 ? 0 : 32; // edge-to-edge on phones, small margin on tablet/desktop
            var padded  = vw - (sidePad * 2);

            if (padded >= cardWidth) {
                // Room for full size (desktop/tablet): no scaling needed
                inner.style.transform = 'none';
                inner.style.width     = '';
                inner.style.height    = '';
                return;
            }

            var scale = padded / cardWidth;
            // offsetHeight is a LAYOUT measurement (unaffected by the
            // transform already applied to #scaleInner), so it stays the
            // true natural height on every call — this prevents the scale
            // from compounding each time the window resizes.
            var cardH = card.offsetHeight;

            inner.style.transform       = 'scale(' + scale + ')';
            inner.style.transformOrigin = 'top left';
            // Shrink the LAYOUT box itself to match the scaled visual size so
            // the flex wrapper centers it correctly with no blank space.
            inner.style.width  = padded + 'px';
            inner.style.height = Math.round((cardH) * scale) + 'px';
        }

        // Run once now, and once more after images/fonts settle (their load
        // can change the card's natural height, which changes the scale math).
        scaleCard();
        window.addEventListener('load', scaleCard);
        setTimeout(scaleCard, 300);
        window.addEventListener('resize', scaleCard);
    }

    /* ══════════════════════════════════════════════════════════════════
       QR CAMERA SCANNER — opens device camera, decodes QR, validates the
       decoded text strictly against the expected report-verification URL
       pattern before accepting it. Anything else is rejected.
       ══════════════════════════════════════════════════════════════════ */

    // Must match exactly: https://rajaiswari.com/search_report.php?huid=XXXXXX
    // where XXXXXX is 6 chars drawn only from the approved HUID charset
    // (no 0, 1, I, O, l — avoids visually ambiguous characters).
    const HUID_CHARSET   = '23456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz';
    const HUID_URL_REGEX = new RegExp(
        '^https:\\/\\/rajaiswari\\.com\\/search_report\\.php\\?huid=([' + HUID_CHARSET + ']{6})$'
    );

    const scanBtn      = document.getElementById('scanBtn');
    const scanModal     = document.getElementById('scanModal');
    const scanCloseBtn  = document.getElementById('scanCloseBtn');
    const scanMsg       = document.getElementById('scanMsg');
    let html5QrCode      = null;
    let scannerRunning   = false;

    function setScanMsg(text, type) {
        scanMsg.textContent = text;
        scanMsg.className = 'scan-modal-msg ' + (type === 'error' ? 'rg-error' : 'rg-info');
    }

    // Extracts and returns the 6-char HUID ONLY if the full scanned string
    // matches the exact expected URL shape; otherwise returns null.
    function extractValidHuid(scannedText) {
        if (!scannedText) return null;
        const match = scannedText.trim().match(HUID_URL_REGEX);
        return match ? match[1] : null;
    }

    function openScanner() {
        if (!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia) {
            alert('Camera scanning is not supported on this browser/device. Please enter the HUID manually.');
            return;
        }
        scanModal.classList.add('rg-open');
        setScanMsg('Point your camera at the QR code on the report.', 'info');

        html5QrCode = new Html5Qrcode('qrReaderBox');
        html5QrCode.start(
            { facingMode: 'environment' }, // prefer rear camera
            { fps: 10, qrbox: { width: 230, height: 230 } },
            onScanFrame,
            () => { /* per-frame decode miss — ignore, keep scanning */ }
        ).then(() => {
            scannerRunning = true;
        }).catch(() => {
            setScanMsg('Camera access denied or unavailable. Please allow camera permission and try again.', 'error');
        });
    }

    function closeScanner() {
        scanModal.classList.remove('rg-open');
        if (html5QrCode && scannerRunning) {
            html5QrCode.stop()
                .then(() => html5QrCode.clear())
                .catch(() => {});
        }
        scannerRunning = false;
        html5QrCode = null;
    }

    function onScanFrame(decodedText) {
        const huid = extractValidHuid(decodedText);
        if (!huid) {
            setScanMsg('This QR code is not a valid Rajaiswari report code.', 'error');
            return; // keep the camera running so the user can try another code
        }
        setScanMsg('Report code recognized — loading...', 'info');
        closeScanner();
        doSearch(huid);
    }

    scanBtn.addEventListener('click', openScanner);
    scanCloseBtn.addEventListener('click', closeScanner);
    scanModal.addEventListener('click', function(e) {
        if (e.target === scanModal) closeScanner(); // click outside box closes it
    });

    /* ── Auto-run search if ?huid= is present in the URL (for QR code scans);
       otherwise show a quiet explanatory placeholder ── */
    (function initFromUrl() {
        const params = new URLSearchParams(location.search);
        const huid = params.get('huid');
        if (huid) {
            doSearch(huid);
        } else {
            renderInitialInfo();
        }
    })();
</script>

</body>
</html>